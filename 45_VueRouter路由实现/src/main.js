/**
 * vue-router vue社区路由插件
 * 内部提供了创建路由对象的方法，以及<router-link>与<router-view>组件
 * 根据传递的routes去创建路由实例，底层会创建路由映射表
 * 监听hashchange事件，通过history.pushState事件，向浏览器的会话历史栈中添加当前路由信息
 * popstate事件，监听浏览器前进与后退操作
 */



